% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/try_again.R
\name{try_again}
\alias{try_again}
\title{Retry code, with a random delay between attempts}
\usage{
try_again(
  x,
  times = 2,
  delay_seconds_min = 5,
  delay_seconds_max = 10,
  verbose = FALSE
)
}
\arguments{
\item{x}{The code to run. \code{try_again()} evaluates it once for each attempt.}

\item{times}{The maximum number of attempts. The default is 2.}

\item{delay_seconds_min}{The shortest delay in seconds. The default is 5.}

\item{delay_seconds_max}{The longest delay in seconds. The default is 10.}

\item{verbose}{Logical. \code{TRUE} reports each failed attempt as a warning, and
a success after a retry as a message. The default is \code{FALSE}.}
}
\value{
\code{TRUE}, invisibly, after the first attempt that succeeds. When every
attempt fails, \code{try_again()} signals the error of the last attempt.
}
\description{
\code{try_again()} evaluates \code{x} up to \code{times} times, and stops at the first
attempt that raises no error. After each failed attempt it waits a number of
seconds drawn from a uniform distribution on
\verb{[delay_seconds_min, delay_seconds_max]}. The delay does not grow with the
attempt number, so this is not exponential backoff.
}
\details{
\code{try_again()} is adapted from \code{try_again()} in testthat. It adds the delay
and the \code{verbose} argument.
}
\examples{
try_again(1 + 1)

# Fail once, then succeed. The delays are 0 so that the example runs fast.
attempt_n <- 0
try_again(
  {
    attempt_n <- attempt_n + 1
    if (attempt_n < 2) stop("not ready yet")
  },
  times = 3,
  delay_seconds_min = 0,
  delay_seconds_max = 0
)
attempt_n
}
\seealso{
\code{vignette("plnr")} for the concepts. \code{try_again()} itself is not
part of the \link{Plan} workflow.

Other utilities:
\code{\link[=create_rmarkdown]{create_rmarkdown()}}
}
\concept{utilities}
