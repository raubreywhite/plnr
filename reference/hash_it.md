# Hash an object

`hash_it()` is internal. It returns `digest::digest(x)`.

## Usage

``` r
hash_it(x)
```

## Arguments

- x:

  The object to hash.

## Value

A character string that holds the hash.
